let attempts = 0;
while(attempts < 10){

  const userName = [( prompt ("Enter username?"))];

for (attempts of userName) {
  if (attempts == "artianz"){
      console.log("Logged In");
  } else if (attempts++) {
  } else {
    (prompt("Enter again"))
   }
  }
  console.log("you've reach the maximum attempts");
}